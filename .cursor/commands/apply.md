Read and follow the full workflow in `.claude/commands/apply.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use the job posting I provide in this message (a URL or pasted job-description text). If I only typed `/apply` with nothing after it, ask me for the URL or the pasted posting before starting.
