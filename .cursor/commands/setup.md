Read and follow the full workflow in `.claude/commands/setup.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use whatever I typed after `/setup` in this message (for example `--section search` or `--section skills`). If I typed nothing extra, run the default full onboarding (Step 0 path selection).
