Read and follow the full workflow in `.claude/commands/add-portal.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use whatever I typed after `/add-portal` in this message (typically the job board's URL). If I typed nothing extra, ask me for the portal URL before starting.
