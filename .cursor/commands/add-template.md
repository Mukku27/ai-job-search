Read and follow the full workflow in `.claude/commands/add-template.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use whatever I typed after `/add-template` in this message (for example `--list`, `--use <name>`, or a path to a `.tex` template). If I typed nothing extra, run the default interactive template registration.
