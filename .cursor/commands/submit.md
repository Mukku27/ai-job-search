Read and follow the full workflow in `.claude/commands/submit.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use whatever I typed after `/submit` in this message (if anything). If I only typed `/submit` with nothing after it, start Wave A from the to-do.
