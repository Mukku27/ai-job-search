Read and follow the full workflow in `.claude/commands/reset.md` exactly, in order.

Wherever that file refers to `$ARGUMENTS`, use whatever I typed after `/reset` in this message (for example `profile`, `documents`, or `all`). If I typed nothing extra, ask me which scope to reset before deleting anything.
